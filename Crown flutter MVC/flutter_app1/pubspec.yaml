# pubspec.yaml
name: ghflutter_demo
description: A demo Flutter app with 5 complete pages (Home, About, Profile, Settings, Details).
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: 1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/Coding in a Creative Workspace.png
